extends: default

rules:
  line-length:
    max: 120

  indentation:
    spaces: 4
    indent-sequences: true

  document-start: enable
  document-end: disable

  colons:
    max-spaces-before: 0
    max-spaces-after: 0

  truthy: false
